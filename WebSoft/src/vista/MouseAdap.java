/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package vista;

import java.awt.event.MouseAdapter;
import java.awt.event.MouseEvent;
import javax.swing.JButton;

/**
 *
 * @author ferchs
 */
public class MouseAdap extends MouseAdapter {
//    @Override
//            public void mouseClicked(MouseEvent e) {
//                int fila = tablaResultados.rowAtPoint(e.getPoint());
//                int columna = tablaResultados.columnAtPoint(e.getPoint());
//                System.out.println(tablaResultados.getModel().getColumnClass(columna).toString());
//                /**
//                 * Preguntamos si hicimos clic sobre la celda que contiene el botón, si tuviéramos más de un botón 
//                 * por fila tendríamos que además preguntar por el contenido del botón o el nombre de la columna
//                 */
//                if (tablaResultados.getModel().getColumnClass(columna).equals(Object.class)) {
//                    JButton btn=(JButton) tablaResultados.getModel().getValueAt(fila, columna);
//                    System.out.println(btn.getActionCommand());
//                }
//            }
}
